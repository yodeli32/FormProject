<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.formpage1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".form3page" android:parentActivityName=".WelcomeActivity">
            <meta-data
                android:name="android.intent.support.PARENT_ACTIVITY"
                android:value=".WelcomeActivity" />
        </activity>

        <activity android:name=".formpage2" android:parentActivityName=".WelcomeActivity">
            <meta-data
                android:name="android.intent.support.PARENT_ACTIVITY"
                android:value=".WelcomeActivity" />
        </activity>

        <activity android:name=".MainActivity" android:parentActivityName=".WelcomeActivity">
        <meta-data
            android:name="android.intent.support.PARENT_ACTIVITY"
            android:value=".WelcomeActivity" />
        </activity>
        
    </application>

</manifest>